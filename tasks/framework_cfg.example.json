{
  "_README": [
    "Per-run framework-hyperparameter OVERRIDES for one autoresearch run (e.g. a Phase-3 OFAT trial).",
    "USAGE: copy this to  runs/<task>/<tag>/framework_cfg.json  and keep ONLY the keys you want to override",
    "       (delete the rest — any omitted key uses the code default). Copying it verbatim = all defaults = no-op.",
    "READ BY (tracked code):  semantic_space.py -> the 'space_initialization' section  ·  got_select.py decide -> the 'got' section  ·  semantic_search.py select -> the 'semantic_search' section  ·  tune_tools.py select-candidate -> tuner.n_min/top_percentile  ·  bo_search.py -> tuner.bo_n_trials + bo_patience_cap/bo_patience_floor (adaptive) or bo_patience (fixed override).",
    "PRIORITY: explicit CLI flag (--catalog / --cfg / --n-min / --top-percentile / --n-trials / --patience) > this file > module default. NOTE: BO patience is adaptive by default = min(bo_patience_cap, max(bo_patience_floor, round(1.5*n_dims))); set tuner.bo_patience to force a fixed value.",
    "SETUP: tools/init_run.py persists --dimension-strategy, --max-evaluations, and --timeout here. For a new run without --timeout it seeds per_runtime_limit from the task.toml run.timeout_seconds value. Evaluation budget and timeout may change on resume; dimension strategy cannot change after semantic artifacts exist.",
    "NOTE: the sections below hold the CURRENT DEFAULTS so this doubles as a reference; '_keys' documents each. 'tuner.K' (warm-config count) is read by the tunable-contract-extractor AGENT (not a deterministic tool), but is injectable here too.",
    "runs/ is gitignored, so the copied file is local-only (not committed)."
  ],
  "got": {
    "n_seed": 5,
    "S": 10,
    "B": 2,
    "C": 1.5,
    "alpha": 0.5,
    "gamma": 0.6,
    "c_pucb": 0.4,
    "c_leaf": 0.4,
    "tau": 0.3
  },
  "space_initialization": {
    "dimension_strategy": "catalog_subset"
  },
  "semantic_search": {
    "policy": "gain_uncertainty_nocost",
    "coverage_weight": 0.1,
    "cost_weight": 0.2,
    "uncertainty_weight": 0.5,
    "deprioritized_budget_interval": 5
  },
  "tuner": {
    "K": 5,
    "K_eval": 3,
    "n_min": 5,
    "top_percentile": 80,
    "bo_n_trials": 40,
    "bo_patience_cap": 20
  },
  "max_evaluations": 200,
  "per_runtime_limit": 60,
  "preflight_runtime_limit": 180,
  "_keys": {
    "got.n_seed": "outer S-GoT: bootstrap roots before PUCB exploit starts (default 5, equal-budget tuned↑ from 3 — the only robust change). OFAT {2,3,5}.",
    "got.S": "outer: stall threshold → inject B fresh candidates (default 10; fresh count = B, no separate m_fresh). OFAT {5,10,20}.",
    "got.B": "outer: PUCB-generation batch size = candidates per PUCB round (default 2; toy preferred 4 but kept 2 for real-loop cost—4 expensive candidates/round). OFAT {1,2,3}.",
    "got.C": "outer: progressive-widening cap coefficient, cap=ceil(C*N^alpha) (default 1.5). OFAT {1.0,1.5,2.5}.",
    "got.alpha": "outer: progressive-widening exponent (default 0.5). OFAT {0.3,0.5,0.7}.",
    "got.gamma": "outer: c_dag lineage influence decay (default 0.6). OFAT {0.4,0.6,0.8}.",
    "got.c_pucb": "outer: PUCB exploration constant over the action pool (default 0.4; flat in [0.1,0.8], base-dependent → kept default). OFAT {0.2,0.4,0.8}.",
    "got.c_leaf": "outer: select_leaf UCB exploration constant during descent (default 0.4). OFAT {0.2,0.4,0.8}.",
    "got.tau": "outer: op-prior softmax temperature (default 0.3). OFAT {0.1,0.3,0.6}.",
    "space_initialization.dimension_strategy": "search-space setup: catalog_subset selects from contracts/semantic-dimensions-v1.json (default); llm_induced makes background-researcher create and fully use a validated run-local dimension_catalog.json before retrieval. Explicit --catalog overrides the source, not the configured full-vs-subset semantics.",
    "semantic_search.policy": "outer semantic point policy, independent of got graph action selection: coverage (fully deterministic, no LLM scores), gain, gain_uncertainty, or gain_uncertainty_nocost (gain_uncertainty without any cost prediction, for when pre-implementation cost estimates are noise). The last three require per-proposal rubric inputs and preserve predicted_gain, uncertainty, cost (absent for gain_uncertainty_nocost), and coverage separately in each record's policy_receipt.",
    "semantic_search.coverage_weight": "semantic acquisition: additive deterministic under-coverage contribution for gain policies (default 0.1).",
    "semantic_search.cost_weight": "semantic acquisition: subtractive predicted implementation/evaluation cost contribution (default 0.2).",
    "semantic_search.uncertainty_weight": "semantic acquisition: exploration bonus used only by gain_uncertainty and gain_uncertainty_nocost (default 0.5); uncertainty is a rubric score, not a calibrated posterior.",
    "semantic_search.deprioritized_budget_interval": "real outer-search budget allocation: every Nth one-based semantic admission is reserved for the best deprioritized proposal when one exists (default 5 = 20%); other slots select only active proposals unless that lane is empty. Must be an integer in [2,1000].",
    "tuner.K": "inner: number of warm-start configs the tunable-contract-extractor PROPOSES per candidate (default 5; read by the AGENT). OFAT {3,5,8} — highest-impact inner knob.",
    "tuner.K_eval": "inner: how many of the K proposed warm configs are EVALUATED at step 0+1 (default 3; best-of-K_eval = screening score). The remaining K−K_eval are DEFERRED — stored params-only in tune_report.phase_a.deferred_configs and evaluated by the deep-tuner FIRST (bo enqueue / grid prepend) ONLY if the candidate is promoted. Saves (K−K_eval) evals on every un-promoted candidate; budget-neutral for promoted ones (deferred are extra initial trials). K_eval>=K disables deferral (evaluate all).",
    "tuner.n_min": "inner: min population before deep-tune fires. NOW DERIVED from top_percentile = ceil(100/(100-P)) (P=80→5); this key is only an explicit OVERRIDE.",
    "tuner.top_percentile": "inner: deep-tune eligibility — best untuned must rank in the top (100-P)% (default 80). OFAT {70,80,90}.",
    "tuner.bo_n_trials": "inner: Phase-C BO trial budget (default 40, raised from 30 per benchmark).",
    "tuner.bo_patience_cap": "inner: cap for the ADAPTIVE BO patience = min(cap, max(12, round(1.5*n_dims))) (default cap 20). low/mid-dim save budget, high-dim ramps to cap. benchmark: ~94% of fixed-20's gain at ~64% cost.",
    "tuner.bo_patience": "inner: OPTIONAL fixed-patience override; if set, disables the adaptive rule above. omit for adaptive. (patience=6 was the main HPO suppressor — don't go below ~12.)",
    "tuner.bo_patience_floor": "inner: floor for adaptive patience (default 12 — must clear the ~startup random period or HPO gets 0).",
    "max_evaluations": "OPTIONAL strict global objective budget = max calls admitted into the task config->score fn. Every tuner atomically reserves one slot in evaluation_attempts.jsonl immediately before score_fn; once the cap is reached no further score call starts, even mid-round or mid-tuner. trials_attempted includes successful and failed admitted score calls; preflight attempts/rejections are reported separately and never consume objective slots. Legacy records without trials_attempted fall back to trials_completed and then warm_start_K.",
    "per_runtime_limit": "OPTIONAL per-evaluation wall-clock limit in SECONDS (one evaluate_config / one train.py run). A config/run exceeding it is hard-killed and recorded as a failed/crashed trial. Enforced by the eval callers, not prepare.py: framework tuners (warmstart_eval/bo/cmaes) run each eval in a timed subprocess; hillclimb uses timed_run.py. The template fallback is 60, but init_run seeds a new real task run from task.toml run.timeout_seconds unless --timeout explicitly overrides it. null/absent = no limit. THIS VALUE IS TASK-SPECIFIC — set it after measuring the task's heaviest legitimate candidate.",
    "preflight_runtime_limit": "OPTIONAL per-config no-score preflight timeout in seconds. Preflight may import/construct a candidate and execute the task-owned smoke hook, but it never calls score_fn or consumes max_evaluations. Defaults to min(180, per_runtime_limit) when omitted."
  }
}
