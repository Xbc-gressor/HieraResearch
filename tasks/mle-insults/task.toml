name = "mle-insults"
description = "Detecting Insults in Social Commentary: public-training validation ROC AUC (scored as 1 - AUC) and a separate final MLE-bench submission."

[env]
type = "uv"
project = "envs/mle"

[run]
working_dir = "tasks/mle-insults"
timeout_seconds = 300


[evaluation]
score_fn = "evaluate_config"
environment_preflight_fn = "preflight_environment"
preflight_fn = "preflight_config"
resource_probe_fn = "resource_probe_config"

[resources]
accelerator = "cuda"
devices = 1
lease_wait_timeout = 600
min_memory_gib = 8

[result]
metric = "one_minus_auc"
required_patterns = ["^score:"]

[constraints]
editable_files = ["train.py", "pyproject.toml"]
readonly_files = ["prepare.py"]
allow_dependencies = true

[mlebench]
competition_id = "detecting-insults-in-social-commentary"
public_data_env = "MLEBENCH_PUBLIC_DATA"
submission = "submission.csv"
